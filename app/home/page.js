export default function LegacyHomePage() {
  return (
    <main className="container" style={{ paddingTop: '7rem' }}>
      <h1>Home (Legacy)</h1>
      <p>Optional route if you used `home.html`. You can remove if not needed.</p>
    </main>
  );
}
